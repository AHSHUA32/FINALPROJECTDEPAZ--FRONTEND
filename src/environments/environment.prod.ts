// Production environment — apiUrl will be replaced with deployed Render backend URL
export const environment = {
    production: true,
    apiUrl: 'https://YOUR-BACKEND-URL.onrender.com' // TODO: Replace with your deployed backend URL
};
